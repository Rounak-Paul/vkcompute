// Chapter 05: Simple increment shader for synchronization demos
#version 450

layout(local_size_x = 256) in;

layout(set = 0, binding = 0) buffer Data {
    float values[];
};

void main() {
    uint idx = gl_GlobalInvocationID.x;
    values[idx] = values[idx] + 1.0;
}
